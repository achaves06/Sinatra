<div>
  <% if session[:player][:total] < 21 && !session[:stay]%>
    <a class="btn btn-success btn-large" href="/hit" role="button" id = "hit">Hit</a>
    <a class="btn btn-danger btn-large" href="/stay" role="button" id = "stay">Stay</a>
  <%end%>
</div>
