<head>
  <script src= "/dealer_cards_and_stats.js"></script>
</head>

<% counter = 1
  session[:dealer][:cards_dealt].each do |value, suit| %>
  <div class = "card" id = "<%= counter %>" style = "opacity: 0;">
    <img src = <%= "/images/cards/#{suit}_#{value}.jpg" %> />
  </div>
  <% counter +=1 %>
<%end%>
